# Web API
fastapi==0.110.0
uvicorn[standard]==0.30.1
gunicorn==23.0.0

# HTTP + upload
requests==2.32.3
python-multipart==0.0.9

# Azure Blob (upload + SAS)
azure-storage-blob==12.20.0
azure-core==1.30.2

# OpenAI (SDK 0.28.x — compatível com openai.api_type/api_base/ChatCompletion)
openai==0.28.1

# (Opcional, mas útil para compatibilidade)
pydantic==2.8.2
typing-extensions==4.12.2

pymongo[srv]>=4.6.0

passlib[bcrypt]==1.7.4
python-jose[cryptography]==3.3.0
